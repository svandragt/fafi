version: "2"
run:
  go: "1.25"
  build-tags:
    - fts5
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
  settings:
    staticcheck:
      checks:
        - "SA*"  # safety-critical: bugs, correctness
        - "S1*"  # simplifications
